import React, { useEffect } from 'react'
import './template.scss'

// level 0 component
export default function Template() {

    useEffect(() => {
        window.scrollTo(0, 0)
    })

    return (
        ""
    )
}